<div class="most_popular" style="background-image: url(<%= asset_path Vote.popular.first.article.image  %>)" >
  <div class="most_popular_title">
      
      <%= Vote.popular.first.article.title %>
  </div>
  <p class="most_popular_body"><%= Vote.popular.first.article.body %></p>

</div>

<div class="grid_container">
  <% @category.each do |category| %>
    <div class="category_containor">
       <%= category.name %>
       <div class="feature">
       </div>
    </div>
</div>
 <% end %>





<%= Vote.popular.first.article.title %>


<h1>Articles index</h1><br>
<h3>Hi,</h3>
<h3><%= current_user.name  %></h3><br>


<ul>
  <% @category.each do |category| %>
    <li>
       <%= category.name %>
      <% category.articles.each do |article| %>
    
      <a href="<%= article_path(article) %>">
        <%= article.title %>
      </a>
    
  <% end %>
    </li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>